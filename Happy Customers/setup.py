from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Predict if a customer is happy or not based on the answers they give to questions asked.',
    author='Ahmad',
    license='',
)
